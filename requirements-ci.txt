# CI dependencies — excludes ultralytics/torch to keep the runner fast.
# The YOLO detector is mocked in tests; these packages are all that's needed.
flask>=3.0.0
numpy>=1.26.0
opencv-python-headless>=4.9.0
pandas>=2.2.0
pytest>=7.4.0
pytest-flask>=1.3.0
